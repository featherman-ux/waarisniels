---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "Over Niels";
const pageDescription = "Lees meer over mij en het verhaal achter Waar is Niels?";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <style>
    :root { 
      --orange: #ffb703; 
      --blue: #4f46e5; /* <-- Updated to the blue from Kaart.astro */
    }

    header.hero {
      background: var(--blue);
      color: #fff;
      text-align: center;
      padding: 3rem 1.5rem;
      margin-bottom: 2rem;
      border-radius: .75rem;
      box-shadow: 0 4px 12px rgba(0,0,0,.1);
    }

    /* Updated: Removed font-family, adjusted size for Inter */
    header.hero h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin: 0;
    }

    /* Updated: Removed font-family, adjusted size/weight for Inter */
    .card h2 {
      color: var(--blue);
      font-size: 1.5rem;
      font-weight: 700;
      margin-top: 0;
    }

    /* Kept the cool grid layout */
    main.container {
      max-width: 900px;
      margin: 0 auto;
      padding: 0 1.5rem 2rem;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1.5rem;
    }

    .card {
      background: #fff;
      border: 1px solid #e5e7eb;
      border-radius: 12px;
      box-shadow: 0 4px 12px rgba(0,0,0,.08);
      padding: 1.5rem 2rem;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    /* Kept the hover effect */
    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 6px 16px rgba(0,0,0,.12);
    }

    /* First card still spans both columns */
    .card:first-child {
      grid-column: 1 / -1;
    }

    .btn-row {
      margin-top: 1rem;
    }
    
    /* Updated: Buttons now use the new blue for better contrast */
    .btn-row a {
      display: inline-block;
      background: var(--blue);
      color: #fff; /* White text on blue is readable */
      padding: .6rem 1rem;
      margin-right: .75rem;
      margin-top: .5rem; 
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600; /* 600 weight from Inter */
      transition: filter 0.2s ease, transform 0.2s ease;
    }
    
    .btn-row a:hover {
      filter: brightness(1.1); /* Simple hover for the new blue */
      transform: scale(1.03);
    }
    
    .hero-img {
      width: 100%;
      border-radius: 12px;
      box-shadow: 0 4px 12px rgba(0,0,0,.1);
      margin-top: 1.5rem;
      margin-bottom: 1rem;
    }

    /* Kept the responsive layout */
    @media (max-width: 640px) {
      main.container {
        grid-template-columns: 1fr; /* Stack on mobile */
      }
      
      header.hero h1 {
        font-size: 2.25rem;
      }

      .card {
        padding: 1.5rem;
      }
    }
  </style>

  <header class="hero">
    <h1>{pageTitle}</h1>
    <p>Deze site wordt herbouwd—maar hier lees je alvast wat over mij.</p>
  </header>

  <main class="container">
    <section class="card">
      <h2>Wie ben ik?</h2>
      <p>Ik ben Niels, mooie baas en codeerheld. Ik deel hier mijn avonturen in Zuid-Amerika en daarbuiten.</p>
      <img
        src="/images/over.JPG"
        alt="Niels onderweg"
        class="hero-img"
        loading="lazy"
      />
      <div class="btn-row">
        <a href="/blog/">Laatste blogposts</a>
        <a href="/map/">Live kaart</a>
      </div>
    </section>

    <section class="card">
      <h2>Wat is Waar is Niels?</h2>
      <p>Een persoonlijke reisblog met interactieve kaart, AI-features en real-time updates voor vrienden en familie.</p>
    </section>

    <section class="card">
      <h2>Contact</h2>
      <p>Mail me of volg me op Instagram!</p>
      <div class="btn-row">
        <a href="mailto:veerman.niels@gmail.com">Mail</a>
        <a href="https://instagram.com/n.veerman" target="_blank" rel="noopener">Instagram</a>
      </div>
    </section>
  </main>
</BaseLayout>