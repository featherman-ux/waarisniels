---
// src/pages/map.astro
import BaseLayout from '../layouts/BaseLayout.astro';
// MapComponent import removed
// getCollection import removed if not needed for other content on this page

const pageTitle = "My Travel Map (Temporarily Unavailable)";
const pageDescription = "The map is currently under maintenance. Please check back later!";

// All map-related data fetching removed
// const postsWithLocation = (await getCollection('blog', ({ data }) => { /* ... */ }));
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <div class="text-center mb-10">
    <h1 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-gray-100">{pageTitle}</h1> {/* Removed font- here too */}
    <p class="mt-4 text-lg text-gray-600 dark:text-gray-400">{pageDescription}</p>
  </div>

  <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-xl">
    <p class="text-center text-lg text-gray-700 dark:text-gray-300">
      We're working on bringing the interactive map back online soon!
    </p>
  </div>

  <div class="mt-8 text-center text-sm text-gray-600 dark:text-gray-400">
    <p>In the meantime, you can explore my blog posts directly.</p>
  </div>
</BaseLayout>